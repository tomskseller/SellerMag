// Страница каталога.
// TODO: перенести сюда вид "catalog" из catalog-product-demo.jsx,
// подключив реальный список товаров через lib/api.js — getProducts().

export default function CatalogPage() {
  return (
    <main className="max-w-6xl mx-auto px-6 py-16 text-center">
      <h1 className="font-display text-2xl font-black" style={{ color: 'var(--ink)' }}>
        Каталог — в разработке
      </h1>
    </main>
  );
}
